Lightweight CSS tooltip in ink, shown on hover/focus. Wrap a single trigger. Keep labels to one short line.

```jsx
<Tooltip label="Trazable a la política §4.2" placement="top">
  <IconButton label="info"><InfoIcon/></IconButton>
</Tooltip>
```

Placements: `top` · `bottom` · `left` · `right`.
