Toggle for binary settings — activate a module, flip a live preference.

```jsx
<Switch label="Módulo CUSTOM" defaultChecked />
<Switch label="Notificaciones" labelPosition="left" />
```

Props: `label`, `labelPosition` (`left`/`right`), plus native checkbox props.
