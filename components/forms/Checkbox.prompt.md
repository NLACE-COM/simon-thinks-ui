Checkbox with optional label and description line.

```jsx
<Checkbox label="Activar memoria de largo plazo" defaultChecked />
<Checkbox label="Modo LIGHT" description="Solo módulos esenciales" />
```

Props: `label`, `description`, plus native checkbox props (`checked`, `onChange`, `disabled`).
