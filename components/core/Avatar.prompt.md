User / agent avatar. Renders an image or falls back to initials on violet.

```jsx
<Avatar name="María Soto" />
<Avatar name="Ana Ruiz" src="/ana.jpg" size="lg" />
<Avatar name="SIMON" square />
```

Props: `name`, `src`, `size` (`sm`/`md`/`lg`), `square`.
